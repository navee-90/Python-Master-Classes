# random function
# In Python, the random module is used to generate random numbers and make random selections — great for simulations, games, or even picking random samples.
import random
num=random.randint(1,10)
print(num)